//#include <stdio.h>

/*
struct person
{
	char name[20];
	int age;
	char phone[30];
};

int main()
{
	struct person p1, p2;

	printf("이름 입력 : ");
	scanf_s("%s", p1.name, sizeof(p1.name));
	printf("%s 의 나이 입력 : ", p1.name);
	scanf_s("%d", p1.age);
	printf("핸드폰 번호 입력(- 같이입력) : ");
	scanf_s("%s", p1.phone, sizeof(p1.phone));

	printf("사람 이름 입력 : ");
	scanf_s("%s", p2.name, sizeof(p2.name));
	printf("%s 의 나이를 입력 : ", p2.name);
	scanf_s("%d", p2.age);
	printf("핸드폰 번호 입력(- 같이입력) : ");
	scanf_s("%s", p2.phone, sizeof(p2.phone));

	printf("p1 이름 : %s", p1.name);
	printf("p1 나이 : %d", p1.age);
	printf("p1 핸드폰 번호 : %s", p1.phone);
	
	printf("p2 이름 : %s", p2.name);
	printf("p2 나이 : %d", p2.age);
	printf("p2 핸드폰 번호 : %s", p2.phone);

	getchar();
	getchar();

	return 0;
}
*/